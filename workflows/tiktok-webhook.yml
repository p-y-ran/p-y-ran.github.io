name: TikTok Webhook Handler
on:
  repository_dispatch:
    types: [tiktok_event]
  workflow_dispatch:

jobs:
  handle-tiktok:
    runs-on: ubuntu-latest
    steps:
      - name: 打印回调数据
        run: echo "收到 TikTok 回调：${{ github.event.client_payload }}"
